{% extends 'AppBundle::layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col s12">
            <h2>Modification du nom</h2>
            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <button type="submit" href="{{ path('scenario_edit', {'id': scenario.id}) }}" class="btn">Modifier</button>
            {{ form_end(edit_form) }}

            <ul class="center">
                <li>
                    <a class="btn" href="{{ path('scenario_history') }}">Retourner à mon historique</a>
                </li>
                <li>
                    <br>
                    <a href="{{ path('scenario_delete', {'id': scenario.id}) }}" class="btn">Suppression</a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}



